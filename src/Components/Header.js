import React from 'react';
import '../css/header.css';

const Header = () => {
	return(

		<div className='header'>
			<h2 className='header'>Keep track of your moneys</h2>
		</div>

		);
}

export default Header;